import React from "react";
import { createRoot } from "react-dom/client";
import "./admin-post-type.css";

document.addEventListener("DOMContentLoaded", () => {
	// const kwtskMetaBox = document.getElementById("kwtsk-post-metabox");
	// if (kwtskMetaBox) {
	// 	const root = createRoot(kwtskMetaBox);
	// 	root.render(<MmdMetaBox kwtskObj={kwtskPostObj} />);
	// }
});
